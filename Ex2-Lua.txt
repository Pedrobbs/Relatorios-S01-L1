function maiorNumero(tabela)
    local maior = tabela[1]
    for i = 2, #tabela do
        if tabela[i] > maior then
            maior = tabela[i]
        end
    end
    return maior
end

local numeros = {5, 12, 7, 33, 2, 18}
print("O maior número é: " .. maiorNumero(numeros))
